int sumar(int,int);
/** \brief genera sumatoria de dos operandos
 *
 * \param primer operando a sumar
 * \param segundo operando a sumar
 * \return suma de los numeros
 *
 */
void refrescarPantalla();
/** \brief espera confirmacion del usuario y borra lo ingresado en la consola
 *
 * \return pantalla borrada
 *
 */
int restar(int,int);
/** \brief genera resta de dos operandos
 *
 * \param primer operando a restar
 * \param segundo operando a restar
 * \return resta de los numeros
 *
 */
float dividir(int,int);
/** \brief genera division de dos operandos
 *
 * \param primer operando tomado como dividendo
 * \param segundo operando tomado como divisor
 * \return cociente de la division
 *
 */
int multiplicar(int,int);
/** \brief genera producto de dos operando
 *
 * \param primer operando tomado como multiplicando
 * \param segundo operando tomado como multiplicador
 * \return producto de la division
 *
 */
