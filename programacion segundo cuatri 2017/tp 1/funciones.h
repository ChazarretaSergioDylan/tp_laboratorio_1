float sumar(float,float);
/** \brief genera sumatoria de dos operandos
 *
 * \param primer operando a sumar
 * \param segundo operando a sumar
 * \return suma de los numeros
 *
 */
float restar(float,float);
/** \brief genera resta de dos operandos
 *
 * \param primer operando a restar
 * \param segundo operando a restar
 * \return resta de los numeros
 *
 */
float dividir(float,float);
/** \brief genera division de dos operandos
 *
 * \param primer operando tomado como dividendo
 * \param segundo operando tomado como divisor
 * \return cociente de la division
 *
 */
float multiplicar(float,float);
/** \brief genera producto de dos operando
 *
 * \param primer operando tomado como multiplicando
 * \param segundo operando tomado como multiplicador
 * \return producto de la division
 *
 */
 int verificar_numero (char []);
/** \brief genera un numero tipo entero para verificar si lo ingresado es un numero entero, flotante o ninguna.
 *
 * \param valor de tipo char[](cadena) de tipo no verificado
 * \return valor de tipo entero siendo 2 para flotante(toma tanto , como . para el ingreso de un flotante),
    1 para entero y 0 para char[] (no numerico).
 *
 */

long long int factorialEntero(int,float);
/** \brief Genera el resultado del factorial del segundo operando
 *
 * \param primer operando tipo entero, si el operando es igual a 1 realiza la opracion
 * \param segundo operando tipo float (entero)
 * \return resultado del factorial del segundo operando de tipo long long int.
 *
 */
